import React from 'react'

import gsap from 'gsap'


const Home = () => {

    return(
        <div>
            home
            {/* 
                create a few section containers, 
                height = 100vh - width = 100vw, 
                scroll and snap effects, 
                effects inside to expand and out sections 
            */}
        </div>
    )
}

export default Home;